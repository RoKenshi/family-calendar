{
  "name": "family-calendar",
  "version": "1.0.0",
  "description": "Family Calendar MVP with Cloudflare Worker and Telegram integration",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
