name = "family-calendar-worker"
main = "worker.js"
compatibility_date = "2024-01-01"

[env.production]
name = "family-calendar-worker"

# Переменные окружения (установите через wrangler secret put или в Cloudflare Dashboard)
# TELEGRAM_BOT_TOKEN - токен вашего Telegram бота
# TELEGRAM_CHAT_ID - ID чата для отправки уведомлений
